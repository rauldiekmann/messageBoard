extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
    p= message.text
    p= message.user
    p= message.added
  button(type="button" onclick="window.location.href='/new'") Go to New Message
